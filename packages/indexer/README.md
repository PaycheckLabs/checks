# Indexer

This package contains Explorer indexing logic and deterministic status reconstruction.

Initial testnet approach:
- Start with lightweight event processing
- Upgrade to The Graph or a DB-backed indexer later if needed
